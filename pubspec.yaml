name: sensorscope
description: SensorScope – Environmental Sensor Dashboard for Pixel Devices
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ^3.16.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0
  fl_chart: ^0.66.0
  sensors_plus: ^4.0.2
  flutter_compass: ^0.7.0
  barometer: ^0.0.4
  light_sensor: ^2.0.0
  battery_plus: ^4.0.2
  geolocator: ^10.1.0
  permission_handler: ^11.1.0
  wifi_scan: ^0.4.1
  flutter_blue_plus: ^1.17.3
  path_provider: ^2.1.2
  csv: ^5.0.2
  intl: ^0.19.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
